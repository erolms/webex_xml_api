require 'spec_helper'

describe WebexXmlApi do
  it 'has a version number' do
    expect(WebexXmlApi::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
